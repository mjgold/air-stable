<%= erb :_header, locals: { object: stall } %>

<!- Should remove these formatting tags and use CSS instead ->
<h2><%= stall.title %></h2>

<i><%= city_state_zip(stall) %></i> - hosted by <%= stall.owner.username %>

<p><%= stall.description %>

<p><button href='/requests/new'>Request Rental</button>

<%= erb :_footer %>
